import { Grid, Typography } from '@mui/material';

const AddDllal = () => {
    const title = 'add';
    return (
        <Grid>
            <Typography>{title}</Typography>
        </Grid>
    );
};

export default AddDllal;
